import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.eniola.vocabadventure",
  appName: "Vocab Adventure",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
